PySide6>=6.6
opencv-python>=4.9
numpy>=1.26
pyodbc>=5.1
pytest>=8.0
# ids_peak / ids_peak_ipl: install separately from the IDS Software Suite (not on PyPI
# in all environments). Required only on machines with the physical camera attached.
